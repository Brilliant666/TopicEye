# RARDAR-AUDIT-FIX-01

历史记录：以下双增长展示已被[2026-09-12修正](2026-09-12-rardar-ranking-content-fix.md)取代；现行Today按唯一主增长全量排序。本文件保留原任务证据，不作为恢复旧展示的要求。

基线：`ec662e1108ad4b37bb4f57f8da41e06cc1d43bd3`。保持双榜Today、历史回顾、Find与动态占位；暂停模块不恢复。

## 修正

- 两源增长独立主显示，累计Star辅助；不合成增长、不声称本系统精确24h。来源状态、采集时间与资料生成时间分别表达。
- 只读指标v2投影补齐原捕获的Trendshift增长/周期/原标签，累计值按健康且未过期、采集时间、固定来源优先级选择；旧原始快照不改写。
- 历史每日上限只限制唯一历史新准入项目。Today与已准入续接按工作片推进，保留全站预算、交互预留、真实失败边界和旧未分类重试记录。
- 现有管理员日程配置返回真正服务进程的有效限额、单位、来源与PID；不能把新Python进程默认值当成旧Runtime实测。
- 结构化形态区分否定、第三方依赖和教程/索引对象。使用原证据做有界派生修订，原Profile/Evidence/generatedAt保持不变。
- 卡片优先简短核心价值，长定位保留；手机栏目/口号分层，已有长解读全宽展示。

## 资料修订操作

在已有后端配置下，`python -m scripts.repair_rardar_material_traits` 默认只列出影响；`--apply`必须明确指定1至16个`--repository`。
只保存既有profile-cache下的内容绑定展示修订，不重新生成Profile。重复操作复用修订，GET不写入；旧资料和原证据保留。未知归属不补造标签。

本轮指定资料范围：gods-eye-view、build-your-own-x、sindresorhus/awesome与tt-a1i/archify。其他潜在变化仅列出，不自动批量修订。

## 验证与交付状态

实现采用审查包真实快照回放和明确标注的模拟回归，二者与真实模型生成区分。审查包未保存原始HTML，不当成源站原字节证明。

定向回归覆盖增长0/缺失/降级、累计规则顺序独立、历史上限后Today续接、合作等待与真实失败、资料修订绑定及复用、正反向形态归属、桌面/手机组件。

最终HEAD CI、真实预览/正式页面、实际进程配置、定向修订和日程入口结果在合并前后记录。当前文档不预先宣称已部署或真实日更全部完成。

回退只恢复代码与受影响的展示派生文件；不得回退已发生的模型预算消耗，不重启原健康数据库。
