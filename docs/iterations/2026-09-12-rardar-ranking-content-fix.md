# RARDAR-RANKING-CONTENT-FIX-02

基线 `4afb199bd93b8f780b1f7a92cbfec3d4b90923db`。本轮明确替代旧的交错展示和双增长主显示；不恢复暂停模块，不更改日预算或日程配额。

## 当前产品规则

- 同一快照先取有效 GitHub reportedDelta（包括0），否则取 Trendshift 增长，否则未知；完整并集按主增长、累计Star、规范仓库名排序，再分页。原始排名和不可变快照不变。
- 卡片和普通详情只突出所选增长，累计Star为辅助；来源、周期、缓存和元数据读取时间保持独立。
- 历史先选最近明确事件日；只有无事件日期时才用采集日。同一期才使用GitHub优先，不跨期拼接。历史列表仍是资料可读优先、累计Star次序，不改为Today排序。
- 不再用能力标题拼接核心价值。已有准确核心价值保留；没有独立价值时，用带证据的项目定位保持原视觉区域。原始资料和生成时间不改写。
- 显式内容修订保存在既有profile-cache，绑定原Profile/Evidence与处理版本；GET只读取验证。实际文字改变不冒充旧深度解读的输入；未受影响的Archify解读继续复用。
- GitHub基础元数据独立于AI，通过现有Today刷新和 `refresh_rardar_boards --metadata-only` 有界补取。坏缓存可重取，单项失败不阻断榜单；不猜许可证，不覆盖历史Star。

## 定向修订入口

在既有后端配置下，`python -m scripts.repair_rardar_material_content` 只预览影响；`--apply` 要求明确列出1至40个 `--repository`（单次操作边界，不是产品库存上限）。保留原Profile、Evidence和旧生成时间，重复执行复用有效修订。普通来源刷新不会触发资料模型生成。

## 验证证据

回归使用用户保存的真实36项Today字段，测试数据标注为历史样本，绝非生产来源。六项指定位置变化全部一致，集合与源排名不变。新增定向回归覆盖主值0/缺失/同值/时区、元数据损坏与恢复、修订绑定与复用、准确核心价值保留。

真实快照预览、最终HEAD CI、正式切换结果在PR与仓库外验收记录中记录；本文件不预先宣称已部署。恢复只针对本轮新增派生文件和代码，不回退历史预算，不重启健康数据库。
